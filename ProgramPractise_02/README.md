# ProgramPractise_01

------------GIT--------------

echo "# ProgramPractise_01" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/moumitacloud08/ProgramPractise_01.git
git push -u origin main
-----------------------

new Thread(deadlock:: worker1,"worker1").start();

new Thread(new Runnable() {
    @Override
    public void run() {
      deadlock.worker1();
    }
},"worker1").start();

---------Java Versions----------------

| Feature                       | First appeared | Status then | Became standard |
| ----------------------------- | -------------: | ----------- | --------------: |
| Switch Expressions            |         JDK 12 | Preview     |      **JDK 14** |
| Text Blocks                   |         JDK 13 | Preview     |      **JDK 15** |
| Records                       |         JDK 14 | Preview     |      **JDK 16** |
| Sealed Classes                |         JDK 15 | Preview     |      **JDK 17** |
| Pattern Matching `instanceof` |         JDK 14 | Preview     |      **JDK 16** |

